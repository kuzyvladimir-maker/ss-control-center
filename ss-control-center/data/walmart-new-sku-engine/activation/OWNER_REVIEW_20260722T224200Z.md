# Owner review — Product Truth activation for Walmart pilot

> Prepared 2026-07-22 from read-only production evidence. This file and the adjacent
> JSON template are **not an approval**. They do not authorize a database migration,
> Product Truth backfill, paid provider call, Walmart feed, delist, reprice or purchase.

## What this is

This updates the structure of the **existing shared production database and donor
catalog**. It does not create a second Bundle Factory, second Product Truth database or
Walmart-only donor catalog.

The current frozen Walmart operator release is:

- directory: `release-artifacts/walmart-new-sku-pilot-engine-2026-07-22-v2`;
- engine SHA-256:
  `8d855372f689653ef4fa245dd5c4696fe0ce5c2f2f3dea157804e24f563896b5`;
- release certificate SHA-256:
  `7879903eb5719367a295c9926e947ea4a10cb4a2da3038bfb6bd52f7db8139a9`.

## Fresh production plan

- Plan: `20260722T224200Z-product-truth-plan-v2/plan.json`
- Plan SHA-256:
  `d36b67276e51a342588c9eecf4e0b5eb06d4630e4da2cce17e8a66a29c8769e3`
- Result: `canApply=true`, `blockers=[]`.
- Exactly eight canonical migrations are pending.
- Known active enrichment, harvest and Product Truth writers: `0`.
- Running queue jobs: `0`; unfinished migrations: `0`; unsettled metered receipts: `0`.
- The plan is read-only and performed no production mutation.

## Fresh recovery snapshot

- Manifest: `backups/20260722T224444Z/backup-manifest.json`
- Manifest SHA-256:
  `84c3b2bfdf625bf5a0b9a40534aab2de590eece79a6f51309f1c054547fa8f02`
- Portable SQLite:
  `backups/20260722T224444Z/production-pre-product-truth.portable.db`
- Portable SHA-256:
  `6e69911d1ab83f77545c9fa5789d35e29f11cd434a25b24d2f03e3fdef952526`
- Size: `257060864` bytes; `integrity_check=ok`; foreign-key violations: `0`.
- The file opens independently with immutable SQLite and requires no WAL/SHM.
- A local plan on the portable file reproduced the production schema, queue, writer,
  migration-set and activation-contract hashes exactly.

Exact approval backup reference:

`local-sqlite-backup://walmart-pilot/20260722T224444Z/production-pre-product-truth.portable.db#sha256=6e69911d1ab83f77545c9fa5789d35e29f11cd434a25b24d2f03e3fdef952526`

## Exact impact of the eight migrations

- Existing product, donor, offer, component and cost rows are not deleted.
- Canonical identity, evidence, listing-scope, budget and operational-run structures
  are added to the same database.
- `128` legacy `EnrichmentJob` rows receive compatibility metadata; their business
  status, target and completed result are preserved. Cancelled by the plan: `0`.
- The old unique `SkuCost_sku_source_effectiveDate_key` index is replaced by
  append-only observation identity and lookup indexes. Existing `SkuCost` rows remain.
- All eight migrations, compatibility updates, receipts and verification run inside
  one write transaction. Any failure before commit rolls back the entire transaction.
- Apply re-checks the exact schema, queue and writer hashes before the first mutation;
  any drift rejects the operation.

## What owner approval would authorize

Only the exact eight schema migrations bound to plan SHA
`d36b67276e51a342588c9eecf4e0b5eb06d4630e4da2cce17e8a66a29c8769e3`.

It would **not** authorize:

- canonical data backfill;
- Oxylabs, Unwrangle or any other paid provider call;
- Walmart lifecycle activation;
- Walmart catalog refresh;
- UPC reservation;
- a Walmart feed or live SKU publication.

The owner decision must also confirm that external writers are paused for the short
activation window. A sealed approval expires no more than 30 minutes after issuance.

## Writer-quiescence audit

The 2026-07-22 source/schedule audit reduced this from an owner-operated technical
task to a Codex preflight assertion:

- `vercel.json` does not schedule `reference-enrichment-worker`,
  `reference-harvest-worker` or `cogs-sweep`;
- all three legacy routes are permanently retired and return HTTP `410`;
- legacy manual reference-catalog harvest/enqueue mutation endpoints are retired;
- canonical Product Truth writers are sealed CLI commands, not background daemons;
- the exact production plan observed enrichment/harvest/operational running counts,
  unfinished migrations and unsettled metered receipts all equal to `0`.

Codex must rerun the plan-bound drift/writer preflight immediately before apply. The
owner is not asked to discover or manually stop technical processes. The plain owner
decision is only whether the already-reviewed exact schema update may be applied; if
any writer appears, the engine rejects the operation before mutation.

## Current stop point

The backup and review packet are complete. Production is unchanged. The next action is
blocked until the owner explicitly approves this exact plan and writer-quiescence
window.
