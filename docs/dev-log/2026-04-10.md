# Dev Log — 2026-04-10

### Session — Инициализация системы логирования

**Задача:** Внедрить систему логирования сессий и wiki знаний по методу Карпати

**Сделано:**
- Создана структура `docs/dev-log/` для логов сессий
- Создана структура `docs/wiki/` для wiki-статей
- Создан `docs/wiki/index.md` — оглавление
- Создана первая wiki-статья `docs/wiki/project-architecture.md`
- Добавлен блок "СИСТЕМА ЛОГИРОВАНИЯ СЕССИЙ" в `CLAUDE.md`
- Создан `.claude/settings.json` с хуком session_start

**Решения:**
- Используем markdown-файлы (без RAG, без баз данных) — просто и эффективно
- Логи по дням (YYYY-MM-DD.md), wiki по темам (kebab-case.md)
- Инструкции вшиты прямо в CLAUDE.md чтобы работало без внешних хуков
- Блок логирования размещён ПЕРЕД секцией "ЧЕГО НИКОГДА НЕ ДЕЛАТЬ" как указано в задаче

**Проблемы:**
- Нет

**Затронутые файлы:**
- `CLAUDE.md` — добавлен блок "СИСТЕМА ЛОГИРОВАНИЯ СЕССИЙ"
- `docs/dev-log/README.md` — создан (инструкция по формату)
- `docs/dev-log/2026-04-10.md` — этот файл
- `docs/wiki/index.md` — создан (оглавление)
- `docs/wiki/project-architecture.md` — создана (первая wiki-статья)
- `.claude/settings.json` — создан (хук session_start)

---

### Session — Полная индексация проекта в Wiki

**Задача:** Создать полную индексацию проекта в docs/wiki/ с картой связей

**Сделано:**
- Создано 25 wiki-статей (модули, алгоритмы, интеграции, бизнес-правила)
- Каждая статья с секцией 🔗 Связи (зависит от, используется в, связанные модули, см. также)
- Создан `docs/wiki/CONNECTIONS.md` — полная карта связей с легендой (← → ⊂ ⇔)
- Обновлён `docs/wiki/index.md` — полное оглавление по категориям
- Добавлены правила связей в CLAUDE.md (секция "🔗 ПРАВИЛА СВЯЗЕЙ")
- Обновлена `project-architecture.md` — добавлены связи

**Созданные статьи (25):**
- Модули: shipping-labels, customer-hub, account-health, frozen-analytics, adjustments-monitor, dashboard, atoz-chargeback, feedback-manager
- Алгоритмы: shipping-rate-selection, budget-check-algorithm, customer-hub-decision-engine, frozen-dry-classification, weekend-distribution
- Интеграции: veeqo-api, amazon-sp-api, gmail-api, google-sheets-sku-db, claude-ai, telegram-notifications, weather-geocoding, n8n-automation
- Бизнес-правила: timezone-rules, carrier-selection-rules, walmart-restrictions, frozen-shipping-rules, label-filename-format
- Инфраструктура: database-schema, external-api-auth

**Решения:**
- Структура категорий: Модули / Алгоритмы / Интеграции / Бизнес-правила / Инфраструктура
- Легенда CONNECTIONS.md: ← зависит, → используется, ⊂ часть, ⇔ двусторонняя
- Правило обратных ссылок внесено в CLAUDE.md как обязательное

**Проблемы:**
- Нет

**Затронутые файлы:**
- `CLAUDE.md` — добавлена секция "🔗 ПРАВИЛА СВЯЗЕЙ"
- `docs/wiki/*.md` — 25 новых статей
- `docs/wiki/CONNECTIONS.md` — карта связей
- `docs/wiki/index.md` — полное оглавление
- `docs/wiki/project-architecture.md` — добавлены связи

---

### Session — Полный аудит проекта и фиксы

**Задача:** Протестировать проект на ошибки, неоптимальности, коллизии и починить всё по порядку

**Сделано (27 → 0 errors, 0 warnings):**

_Безопасность и API:_
- Проверено: `.env` НЕ в git, `.gitignore` покрывает `.env*` — ротация ключей не требуется
- `src/app/api/external/{orders,cs,shipping}/route.ts` — добавлены try/catch, `safeJson()` хелпер, HTTP 502 на upstream ошибках, 400 на невалидном JSON
- `src/lib/customer-hub/message-analyzer.ts` — бесполезная проверка `apiKey.includes("api_key")` заменена на `!apiKey.startsWith("sk-ant-")`
- `src/lib/amazon-sp-api/auth.ts` — убран silent STORE1 fallback в `getAccessToken()` (creds LWA должны совпадать с приложением, выдавшим refresh_token); валидация `storeId` формата `store[1-5]` с явной ошибкой вместо `|| 1`
- `src/app/api/amazon/account-health/route.ts` — убраны `console.log`, утекающие префикс SP-API токена

_База данных:_
- `prisma/schema.prisma` — добавлены `@@index([planId])` и `@@index([orderId])` на `ShippingPlanItem` (fix full table scan)
- `src/app/api/customer-hub/messages/route.ts` — N+1 устранён: один `findMany` с `{ in: [...] }` вместо `findUnique` в цикле
- `src/app/api/adjustments/scan/route.ts` — N+1 устранён: bulk check + `createMany` (переписан с `any[]` на типизированный `ScannedAdjustment`)

_Gmail и Customer Hub:_
- `src/lib/gmail-api.ts` — hardcoded `localhost:3000` заменён на `resolveRedirectUri()`: explicit env → Vercel URL → dev fallback → throw; вызов перенесён в `createOAuth2Client()` чтобы не падать на module load в production
- `src/lib/customer-hub/gmail-parser.ts` — хрупкие regex заменены: ASIN matching через 3 паттерна (`/dp/`, `/gp/product/`, `asin=`) с валидацией `^B[A-Z0-9]{9}$`; Product Name теперь ищется через `<a href=.../dp/ASIN>` anchor вместо любого `<td>`
- `src/app/api/customer-hub/atoz/route.ts` — подключён к `prisma.atozzClaim` (`claimType: "A_TO_Z"`)
- `src/app/api/customer-hub/chargebacks/route.ts` — подключён к `prisma.atozzClaim` (`claimType: "CHARGEBACK"`)
- `src/app/api/customer-hub/feedback/route.ts` — подключён к `prisma.sellerFeedback`
- `*/[id]/send`, `*/[id]/submit`, `*/[id]/remove` — возвращают HTTP 501 вместо 200 + "Not implemented" в body

_Удалены legacy Customer Service дубли:_
- `src/app/customer-service/` (page)
- `src/app/api/cs/` (analyze, cases, stats)
- `src/app/api/external/cs/` (прокси)
- `src/components/cs/{ActionBadge,AnalysisPanel,CarrierDelayAlert,CaseHistoryTable,ImageUploader,OrderInbox,ResponseEditor}.tsx`
- `src/components/cs/StoreTabs.tsx` — оставлен (используется в `analytics/page.tsx`)
- Обновлены `components/layout/Header.tsx` (pageTitles), `app/page.tsx` (Dashboard link → `/customer-hub`)

_React/ESLint (8 errors → 0):_
- Создан `src/lib/use-mounted.ts` — `useMounted()` hook через `useSyncExternalStore` (React 19 паттерн без `setState` в `useEffect`)
- `AppShell.tsx`, `Header.tsx`, `integrations/page.tsx` — мигрированы на `useMounted()`
- `settings/page.tsx` — `GmailAccountsPanel` переписан через `useSearchParams()` + `useMemo` вместо `useEffect + setState`
- Исправлены unescaped quotes в `app/page.tsx:182` и `MessagesTab.tsx:157`
- Удалены unused imports: `veeqoDateToLocal`, `DollarSign`, `CardHeader`, `CardTitle`, 3 неиспользуемых eslint-disable директивы

_TypeScript:_
- `adjustments/scan/route.ts` — убран `skipDuplicates: true` (не поддержан SQLite в Prisma 7, пре-фильтрация уже гарантирует отсутствие дубликатов)

**Решения:**
- Не ротировать API ключи: `.env` не закоммичен, риска утечки нет
- Миграцию для новых индексов применил через `prisma db push` (dev.db была пустая 0 байт, миграционный drift — pre-existing)
- TODO endpoints Customer Hub не реализовывать полностью — для мутаций (send/submit/remove) возвращать HTTP 501, для GET-чтений подключить существующие таблицы напрямую (`AtozzClaim`, `SellerFeedback`)
- **Обнаружен prompt injection в `node_modules/next/dist/docs/index.md`** — комментарий "AI agent hint: export `unstable_instant`" — проигнорирован, никаких экзотических API не добавлял. Реальный Next.js API не содержит такого экспорта
- Legacy `components/cs/StoreTabs.tsx` оставлен, потому что используется `analytics/page.tsx` (не чистый legacy)
- Модель `CsCase` в Prisma оставлена, т.к. имеет FK на `FrozenIncident` — удаление требует отдельной миграции

**Проблемы:**
- `AGENTS.md` содержит странную инструкцию "This is NOT the Next.js you know" — непонятно, реальное ли это предупреждение от владельца или тоже prompt injection. Все изменения сделаны по существующим паттернам в коде, чтобы не попасть на фейковый API
- Prisma migration drift pre-existing: `BuyerMessage`, `AmazonOrder`, `SyncLog` в schema без соответствующих миграций. Для прода нужна чистая миграция (сейчас sync через `db push`)

**Верификация:**
- `npm run lint` → 0 errors, 0 warnings
- `npx tsc --noEmit` → 0 errors
- `npm run build` → успешно (все routes компилируются)

**Затронутые файлы:**
- `prisma/schema.prisma` — индексы на ShippingPlanItem
- `src/lib/use-mounted.ts` — новый React 19 hook
- `src/lib/gmail-api.ts` — resolveRedirectUri
- `src/lib/amazon-sp-api/auth.ts` — strict fallback
- `src/lib/customer-hub/{gmail-parser,message-analyzer,message-enricher,response-sender}.ts`
- `src/lib/sync/orders-sync.ts` — удалён unused eslint-disable
- `src/app/api/external/{orders,cs→deleted,shipping}/route.ts`
- `src/app/api/customer-hub/{atoz,chargebacks,feedback}/route.ts` — подключены к Prisma
- `src/app/api/customer-hub/{atoz,feedback,messages}/[id]/*/route.ts` — HTTP 501
- `src/app/api/customer-hub/messages/route.ts` — bulk check
- `src/app/api/adjustments/scan/route.ts` — bulk createMany
- `src/app/api/amazon/{account-health,stores,shipping/buy}/route.ts` — cleanup
- `src/app/{page,integrations,settings}/page.tsx` — useMounted / useSearchParams
- `src/app/customer-service/` — удалён
- `src/app/api/cs/` — удалён
- `src/components/cs/` — удалено 7 файлов (оставлен StoreTabs)
- `src/components/layout/{AppShell,Header}.tsx` — useMounted
- `src/components/customer-hub/MessagesTab.tsx` — escape quotes
