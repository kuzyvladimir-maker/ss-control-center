/**
 * RETIRED 2026-07-18.
 *
 * This legacy entrypoint could enumerate implicit scope, fan out listings, and
 * write without the sealed Product Truth run/queue/ledger contract. It remains
 * only as an explicit fail-closed tombstone for old commands and documentation.
 */

throw new Error(
  "LEGACY_COGS_BATCH_RETIRED: use `npm run product-truth -- doctor|plan|execute|resume|status|report`",
);
